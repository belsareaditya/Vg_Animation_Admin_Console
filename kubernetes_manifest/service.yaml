apiVersion: v1
kind: Service
metadata:
  name: vault-guard-nodeport
spec:
  type: NodePort
  selector:
    app: vault-guard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4173
      nodePort: 31080  # optional: explicit node port; otherwise k8s assigns one
